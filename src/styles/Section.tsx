import { PALETTE } from "@/app/theme/styles/constantes";
import { SxProps } from "@mui/material";

export const section: SxProps = {
  py: "80px",
};
export const section_gray: SxProps = {
    py: "80px",
    backgroundColor: PALETTE.GRAY_300,
  };
